name: Keep HF Space Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: curl -s https://aetherporium-expert-agent.hf.space > /dev/null 2>&1 || true
