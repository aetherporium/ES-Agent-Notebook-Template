name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s https://huggingface.co/spaces/aetherporium/expert-agent > /dev/null 2>&1 || true
